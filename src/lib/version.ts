// Bumped by hand on every commit (patch for fixes/small changes, minor for
// new features, major for breaking/large rewrites). Shown on the admin
// login page and in the admin/author sidebars so anyone can tell which
// build is live.
export const APP_VERSION = "1.1.3";
